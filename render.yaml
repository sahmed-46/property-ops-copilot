services:
  - type: web
    name: property-ops-copilot
    runtime: docker
    plan: free
    healthCheckPath: /ready
    envVars:
      - key: DATABASE_PATH
        value: data/runtime/property_ops.db
